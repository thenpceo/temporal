node_modules
.next
out
dist

# Local env (never commit — contains ANTHROPIC_API_KEY and any Slack webhook)
.env
.env.local
.env.*.local

# Runtime state (generated by the worker / API at runtime)
.playbook.json
.bigquery-fail-next

# OS / editor
.DS_Store
.idea
.vscode

# Logs and intermediates
*.log
.temporal
.tsbuildinfo
tsconfig.tsbuildinfo

# Next.js auto-generated
next-env.d.ts

# Claude Code preview config (not part of the repo)
.claude

# Private — not for the public repo
docs/demo-script.md
